defaults:
  - algorithm: ippo
  - env: clean_up
  - wandb: default
  - _self_

seed: 0
dry_run: false

hydra:
  run:
    dir: .
  output_subdir: null
  job:
    chdir: false
    env_set:
      PYTHONPATH: .
